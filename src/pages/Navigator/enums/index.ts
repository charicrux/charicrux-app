export namespace Screens {
    export enum Initial {
        LAUNCH = "launch"
    } 
    export enum Account {
        CREATE = "create-account",
        LOGIN = "LOGIN"
    }
    export enum Token {
        CREATE = 'create-token'
    }
}