export namespace Screens {
    export enum Initial {
        LAUNCH = "launch"
    } 
    export enum Account {
        CREATE = "create-account",
        LOGIN = "LOGIN",
        ORGANIZATIONS = "organizations",
        VIEW = 'view'
    }
    export enum Token {
        CREATE = 'create-token'
    }
    export enum TAB_NAVIGATOR {
        INITIAL = "navigator"
    }
}